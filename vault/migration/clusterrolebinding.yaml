apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: vault-migration-secret-reader
subjects:
  - kind: ServiceAccount
    name: vault-migration
    namespace: vault
roleRef:
  kind: ClusterRole
  name: vault-migration-secret-reader
  apiGroup: rbac.authorization.k8s.io
